# career-ops

**⚠️ This installer is disabled in this personal fork.** See `core/AGENTS.md` for how this checkout relates to the original project. The `npx` commands below no longer work.

---

One-command installer for career-ops — the AI-powered job search pipeline built on Claude Code.

```bash
npx @santifer/career-ops init
```

(This command is disabled in this fork — see above.)

## Usage

```bash
npx @santifer/career-ops init [folder]   # default folder: ./career-ops
```

(This command is disabled in this fork.)

For manual setup, use `git clone` as described in the original project documentation.

## Requirements

- Node.js 18+
- git

## License

MIT © Santiago Fernández de Valderrama
